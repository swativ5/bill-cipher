Mathematical mod:
```
int mod(int a, int b)   {
    return ((a % b) + b) % b;
}
```
String input
```
cout << "Enter your word: ";
cin.ignore();
getline(cin, plain);
```

Convert to char to binary
```
<bitset<8>(c).to_string()>
``` 

